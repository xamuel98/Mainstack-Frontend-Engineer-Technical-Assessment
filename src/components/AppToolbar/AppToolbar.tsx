import React from 'react';
import { AppToolbarContainer } from './AppToolbar.styles';

const AppToolbar = () => {
    return (
        <AppToolbarContainer>
            <h1>Hello</h1>
        </AppToolbarContainer>
    );
};

export default AppToolbar;
