import React from 'react';
import { Box } from '@chakra-ui/react';
import Empty from '@/components/Empty';

const Home: React.FC = () => {
    return (
        <Box>
            <Empty />
        </Box>
    );
};

export default Home;
